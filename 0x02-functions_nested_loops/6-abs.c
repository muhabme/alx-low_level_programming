#include "main.h"

/**
* _abs - function give the absolute
*
* @n: takes in integer type input for function
*
* Return: always postive
*/
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
